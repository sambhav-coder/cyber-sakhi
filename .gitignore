node_modules
.env
.env.local
.env.example
.env*.local
.next/
*.tsbuildinfo

.trae/
.claude/

# Hardhat / Solidity build artifacts and deployment outputs (regenerable)
blockchain/artifacts/
blockchain/cache/
blockchain/typechain-types/
blockchain/deployed/

# Local Sakhi Network store (fingerprints only, still never commit)
.data/

# Local AI voice/STT/TTS models
models/
!models/

# ...but keep the compiled ML email classifier artifact (product-critical,
# small, regenerable via scripts/ml/train.py). Raw training data stays out.
!models/ml/
!models/ml/email_classifier.json

# ML training data & venvs (regenerate via scripts/ml/download.py)
data/
.ml-venv/

build.log
dev-server.log
dev-server-qlock.out.log

# Temporary debug scripts and logs
csb-debug*.mjs
csb-shot-tmp.mjs
dev-server-repro*.log
probe-*.cjs
probe-*.mjs
repro-*.mjs
smoke-*.mjs
verify-*.mjs
inspect-*.mjs

# Temporary screenshots
task2-*.png

# Backup config files
*.backup

# Temporary fixture files not in tests/

# Python generated cache
__pycache__/
*.pyc

# Browser/session cookies - never commit
cookies*.txt

# Local/temporary Cyber-Sakhi backups
.avatar-backup-*/
gov-local-changes-backup.patch
staged-files.txt

# Local voice model directories
models/piper/
models/vosk/

# Browser/session cookies
cookies*.txt
