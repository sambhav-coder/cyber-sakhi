import { requireGovPage } from "@/lib/gov/govGuard"; import { GovDataView } from "@/components/gov/GovDataView"; export const dynamic = "force-dynamic"; export default async function Page(){await requireGovPage("case.view_meta"); return <GovDataView title="Investigation Queue" description="New, triage, and risk-priority work, scoped to your jurisdiction." endpoint="/api/gov/queue?view=PRIORITY"/>;}
