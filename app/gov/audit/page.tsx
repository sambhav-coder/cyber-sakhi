import { requireGovPage } from "@/lib/gov/govGuard"; import { GovDataView } from "@/components/gov/GovDataView"; export const dynamic = "force-dynamic"; export default async function Page(){await requireGovPage("audit.view"); return <GovDataView title="Audit Logs" description="Immutable government access and workflow records." endpoint="/gov/api/audit?page=1&pageSize=50"/>;}
