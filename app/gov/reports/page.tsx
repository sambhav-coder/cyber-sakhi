import { requireGovPage } from "@/lib/gov/govGuard"; import { GovDataView } from "@/components/gov/GovDataView"; export const dynamic = "force-dynamic"; export default async function Page(){await requireGovPage("report.generate"); return <GovDataView title="Reports" description="Live scoped report preview. CSV exports are recorded in the audit ledger." endpoint="/api/gov/reports"/>;}
