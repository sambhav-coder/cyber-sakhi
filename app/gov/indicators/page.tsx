import { requireGovPage } from "@/lib/gov/govGuard"; import { GovDataView } from "@/components/gov/GovDataView"; export const dynamic = "force-dynamic"; export default async function Page(){await requireGovPage("indicator.view"); return <GovDataView title="Indicator Intelligence" description="Search indicators using the search parameter; correlations mean possible shared indicator, never confirmed attribution." endpoint="/api/gov/indicators?q=example.invalid"/>;}
