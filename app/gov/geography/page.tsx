import { requireGovPage } from "@/lib/gov/govGuard"; import { GovDataView } from "@/components/gov/GovDataView"; export const dynamic = "force-dynamic"; export default async function Page(){await requireGovPage("geo.view"); return <GovDataView title="Geographic Intelligence" description="Aggregate State → District → locality metrics; no exact victim locations are rendered." endpoint="/api/gov/geo"/>;}
