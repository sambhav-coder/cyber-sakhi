import { requireGovPage } from "@/lib/gov/govGuard"; import { GovDataView } from "@/components/gov/GovDataView"; export const dynamic = "force-dynamic"; export default async function Page(){await requireGovPage("analytics.view"); return <GovDataView title="Analytics & Trends" description="Current period compared with the preceding equal period; an emerging trend requires ≥30% growth." endpoint="/api/gov/trends?days=30"/>;}
